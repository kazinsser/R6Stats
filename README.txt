Ubisoft requires a username and password to call their API. Create a settings.ini file with the following three lines and set it to gitignore so your credentials don't get pushed:
[uplay-login]
username =
password =


Packages:
r6sapi
gspread
oauth2client


# Testing git exclude on raspberry pi.
